import React, { Component } from "react";
import "./styles/Chat.scss";
class Chat extends Component {
  render() {
    return (
      <div className="main">
        <div className="kalai-split">
          <div className="left-kalai">
            <div className="box-kalai">
              <div className="box-left">
                <div className="inside-boxes">
                  <div className="box-inside-left col-sm-10 col-md-12 col-lg-12 col-xl-12 col-xxl-12">
                    <p>
                      To request multiple images of the same size in your
                      browser, add the random query param to prevent the images
                      from being cached:
                    </p>
                    <p>
                      To request multiple images of the same size in your
                      browser, add the random query param to prevent the images
                      from being cached:
                    </p>
                  </div>
                  <div className="box-inside-right col-sm-10 col-md-12 col-lg-12 col-xl-12 col-xxl-12">
                    <img src="https://picsum.photos/200/300" />
                  </div>
                </div>
              </div>
              <div className="box-right">
              <p>
                      To request multiple images of the same size in your
                      browser, add the random query param to prevent the images
                      from being cached:
                    </p>
                    <p>
                      To request multiple images of the same size in your
                      browser, add the random query param to prevent the images
                      from being cached:
                    </p>
              </div>
            </div>
          </div>
          <div className="right-kalai">
          <p>
                      To request multiple images of the same size in your
                      browser, add the random query param to prevent the images
                      from being cached:
                    </p>
                    <p>
                      To request multiple images of the same size in your
                      browser, add the random query param to prevent the images
                      from being cached:
                    </p>
                    <p>
                      To request multiple images of the same size in your
                      browser, add the random query param to prevent the images
                      from being cached:
                    </p>
                    <p>
                      To request multiple images of the same size in your
                      browser, add the random query param to prevent the images
                      from being cached:
                    </p>
                    <p>
                      To request multiple images of the same size in your
                      browser, add the random query param to prevent the images
                      from being cached:
                    </p>
                    <p>
                      To request multiple images of the same size in your
                      browser, add the random query param to prevent the images
                      from being cached:
                    </p>
          </div>
        </div>
      </div>
    );
  }
}

export default Chat;
